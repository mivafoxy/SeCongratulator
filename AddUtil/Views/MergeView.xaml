<Window x:Class="AddUtil.Views.MergeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MergeView" Height="100" Width="300" ResizeMode="NoResize">
    <Grid>
        <StackPanel x:Name="OldDbPathZone" 
                    Grid.Row="0" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            
            <Button x:Name="DoMergeButton" 
                    Content="Переместить" 
                    Margin="5" 
                    FontSize="20"
                    Command="{Binding RunMergeCommand}"/>
            
            <Button x:Name="AbortButton" 
                    Content="Отмена" 
                    Margin="5" 
                    FontSize="20"
                    Command="{Binding AbortCommand}"/>
        </StackPanel>
    </Grid>
</Window>
